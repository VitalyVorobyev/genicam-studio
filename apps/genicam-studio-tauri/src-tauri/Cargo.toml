[package]
name = "genicam_studio_tauri"
version = "0.1.0"
description = "GenICam Studio desktop app"
edition = "2021"
build = "build.rs"

[dependencies]
# Tauri v2.
# Frontend JS bindings live in `@tauri-apps/api` (also v2); keep both in sync.
tauri = { version = "2", features = [] }

[build-dependencies]
tauri-build = { version = "2", features = [] }
