{
  // Studio config: connects to the camera service on its fixed port.
  // Service must be started first with zenoh-local.json5.
  mode: "peer",
  connect: {
    endpoints: ["tcp/127.0.0.1:7447"]
  },
  scouting: {
    multicast: { enabled: false }
  }
}
