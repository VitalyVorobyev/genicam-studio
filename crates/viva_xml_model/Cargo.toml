[package]
name = "viva_xml_model"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
viva-genapi-xml = { version = "0.2.0", default-features = false }
viva-genapi = "0.2.0"

[dev-dependencies]
