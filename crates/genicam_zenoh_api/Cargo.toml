[package]
name = "genicam_zenoh_api"
version = "0.1.0"
edition = "2021"
description = "Shared Zenoh API payload types for GenICam Studio (no Zenoh dependency)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
